# docker-compose.public.yml - standalone, world accessible
services:

  mongo:
    ports:
      - ${COMPOSE_PROJECT_PORT}:27017

networks:
  net:
    driver: bridge
    name: ${COMPOSE_PROJECT_LEAD}-${COMPOSE_PROJECT_CNET}